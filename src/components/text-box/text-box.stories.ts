import { html } from 'lit-html';

export default {
  title: 'Components/Text Box',
  component: 'text-box',
};

const Template = () => html`
  <text-box>
  </text-box>
`;

export const Default = Template.bind({});
